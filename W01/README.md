# 第一周、第二周學習紀錄

## 第一周
- 學會使用 VS Code 複製儲存庫至本機
- 完成第一支 Python 程式 W01 | GitHub × VS Code

## 第二周
- 作品名稱：Class Score Analyzer
- 我要解決的目標
    - 透過程式快速統計資料
- 使用到的 Python 技術 
    - len(), sum(), max()...等內建工具
- 我遇到的問題與如何解決
    - **問題**：目前的 `sorted(reverse=True)[:3]` 如果有同分狀況(例如前四名分數分別是：97, 92, 92, 92)，只會抓出 `[97, 92, 92]`，漏掉第三個 92 分

    * **解決方案**：如果要精準處理「名次」不是單純「前三個元素」，可改用 `set()` 去重後排序找出前三高分
- 我學到了什麼
    - 如何使用內建工具
